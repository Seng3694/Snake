GameState = Class:new()
GameState.transparent = false

function GameState:load()
end

function GameState:reload()
end

function GameState:update(dt)
end

function GameState:draw()
end

function GameState:keypressed(key)
end

function GameState:keyreleased(key)
end

function GameState:mousemoved(x, y, dx, dy)
end

function GameState:mousepressed(x, y, button, presses)
end

function GameState:mousereleased(x, y, button, presses)
end